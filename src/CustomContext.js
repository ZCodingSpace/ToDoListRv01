import { createContext } from "react";

// Create a context for managing the theme
export const ThemeContext = createContext();

// Create a context for managing the todo list data
export const DataContext = createContext();
